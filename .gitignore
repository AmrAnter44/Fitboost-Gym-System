# Dependencies
node_modules/
/.pnp
.pnp.*

# Next.js
.next/
out/

# Build artifacts
dist/
release/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local
!.env.example

# Database files (تحتوي على بيانات حساسة - لا ترفع على Git)
prisma/*.db
prisma/*.db-journal
prisma/*.db.backup*
prisma/*.autobackup.*
prisma/*.pre-migrate-*
prisma/*.integrity_backup
prisma/**/*.db
prisma/**/*.db-journal
*.db-journal

# 🔒 أي ملف قاعدة بيانات أو ملف مؤقت/نسخة منها — بأي لاحقة كانت.
# القواعد القديمة كانت بتفوّت حاجات زي gym.db.bak-before-date-fix و gym.db-wal
# و gym.db.corrupt-snapshot، وده كفيل يسرّب بيانات كل الأعضاء على ريبو عام.
prisma/*.db*
prisma/**/*.db*
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.corrupt-snapshot
*-HEALTHY*

# 🔒 ملفات إكسل — غالباً فيها إيرادات أو بيانات أعضاء
*.xlsx
*.xls

# Member photos (بيانات شخصية حساسة - لا ترفع على Git)
prisma/photos/

# WhatsApp session credentials (سرية - تعطي تحكم كامل في حساب الواتساب)
.baileys_auth/
.wwebjs_cache/
.wwebjs_auth/

# Client / assistant electron builds
dist-client/

# Test artifacts
test-results/
playwright-report/

# Electron builds
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.snap
*.msi

# Logs
*.log
logs/
dev.log

# IDE & OS
.vscode/
.idea/
.vs/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
tmp*
nul

# Uploads
public/uploads/

# Archived data (sensitive)
public/photos.zip
public/photos/*.zip
data/archived/
/test-supabase.js.bak
/.internal-api-token

# Sensitive scripts / old backups (never commit)
*.db.backup*
*.integrity_backup
prisma/gym.db.pre-vacuum-*
*.bak
prisma/backups/
prisma/prisma/

# Claude Code settings
.claude/settings.local.json

# Chromium resources (too large for GitHub)
resources/

# Rendered video output (must never be committed)
FFOutput/
*.mp4

# مانيفست باكاب الصور السحابي (يتولد محليًا)
cloud-photos-manifest.json
